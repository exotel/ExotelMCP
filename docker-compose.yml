services:
  exotel-mcp:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - SSL_VERIFY=true
      - CALLS_FORCE_HTTP=false
    volumes:
      - mcp-data:/app/data
      - mcp-logs:/app/logs
    restart: unless-stopped

volumes:
  mcp-data:
  mcp-logs:
